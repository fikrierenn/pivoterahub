# 2026-05-13 — Auth + Plan 02/03 + Kod Analizi + Agent Sistemi + FrameOS Temizliği

Yoğun çok-fazlı oturum. 37+ commit. Aşağıdaki başlıklar sırasıyla ele alındı.

---

## Tamamlananlar

### 1. Toplu commit organizasyonu (oturum başı)
98 uncommitted dosya `commit-splitter` agent ile 9 mantıksal commit'e bölündü:
- `d30f061` Core API + backend
- `55338b3` Pages & UI components
- `af943e2` LLM modules (Gemini, competitor, scraper)
- `c157ac9` New API routes
- `3ea6db6` Components & charts
- `54f74ef` Config & migrations
- `dd48df9` Spec docs
- `41e5024` Build config + .gitignore (debug dosya pattern'leri)
- `472f31a` Scripts

### 2. Plan 01 — Auth + Altyapı (FAZ 1)
- `c1d9665` logger + in-memory rate limiter (`lib/logger.ts`, `lib/rateLimit.ts`)
- `7aa1e0b` next-auth v4 CredentialsProvider + middleware (API → 401 JSON, sayfa → /login redirect)
- `e8e147a` `enforceRateLimit` guard helper + 5 ana AI endpoint'e uygulandı
- `.env.local` dev credentials: `admin@pivoterahub.com` / `changeme-2026`

### 3. Tip uyumu + teknik borç temizliği
- `50a50c1`, `87dfdf2`, `b3b590d` — T-02/T-03/T-04 + 10+ minor TS hatası kapandı
- `funnel_stage` enum 4 değere düşürüldü, `CompetitorData.error?` eklendi
- `complete-analysis` literal `\n` + undefined `profile` düzeltildi

### 4. `.kiro/specs/` → `docs/specs/` taşıma + file-size-discipline kuralı
- `e1f2f41` `.kiro` kaldırıldı, 7 spec docs/specs altında
- `e511bd7`, `e58b03e` `file-size-discipline.md` (reporthub stili: 300 yumuşak, 500 kırmızı çizgi, "touch ettikçe azalt") — PivotaraHub snapshot 5 dosya

### 5. Plan 02 — Form/Analiz Konsolidasyonu
- `bfdbf39` (Faz A) `ClientInfoEditor` silindi (intake formu ile aynı veri, drive-by edit)
- `60d0c39` (Faz B) `/analysis` page'inden Instagram + Competitor sekmeleri kaldırıldı (kendi sayfaları zaten var) — 469→304 satır
- `c011277` (Faz C) `/api/clients/[id]/analyze` 501 placeholder silindi
- `9c6b5b9` (Faz D) Detail page header'da 6 buton → "Analizler ▾" dropdown (native `<details>`)

### 6. Plan 03 — complete-analysis Paralelleştirme
- `fc4ebd0` Zincir 1 (consulting LLM 4 adım) + Zincir 2 (Instagram) `Promise.all` ile paralel
- Zincir 3 (Rakip) Instagram'a bağımlı — sonra çalışır
- Partial-success: `partial_failures[]` + `duration_ms` field response'a eklendi
- Sahte mock data (Edirne emlak danışmanı) kaldırıldı
- 353 → 284 satır

### 7. Optimizasyon Agent Sistemi
- `a35bbb4` 4 optimizasyon agenti eklendi: `code-optimizer`, `bundle-guardian`, `nextjs-component-reviewer`, `code-simplifier` (FrameOS + template kaynakli, generic'leştirildi)
- `23759ba` 4 agent PivotaraHub için özelleştirildi — proje-özel bağlam blokları (AI provider Tier hiyerarşi, costTracking zorunluluğu, hot path tespitleri, next-auth pattern, SDK listesi)
- Template'e de 3 generic agent eklendi (`bf22263`)
- ReportHub'a `code-optimizer-dotnet` versiyonu yazıldı (350 satır, EF Core + SP + async + caching odaklı)

### 8. code-optimizer raporu uygulaması (Tier-2 perf)
- `08c99e1` clients/[id]/page race condition + focus listener çift fetch kaldırıldı
- `video-analysis.ts:481` `fs.writeFileSync` → `fs.promises.writeFile` (event loop)
- `complete-analysis/route.ts` duplicate dynamic import → static (hot path)

### 9. Kapsamlı kod analizi + uygulama
3 paralel agent (`bundle-guardian` + `nextjs-component-reviewer` + security review) çalıştırıldı, bulgular uygulandı:
- `02b734d` **XSS sanitizer** — `<SafeHtml>` component (DOMPurify), 33 yerde `dangerouslySetInnerHTML` sarmalandı
- `cf83642` 3 unused paket uninstall (`@anthropic-ai/sdk`, `groq-sdk`, `puppeteer` — ~250MB+) + VideoAnalysisForm 3 yerde dynamic import (Header dahil, ~50-100KB First Load JS düşüşü)
- `e25c97a` **SessionProvider** wrapper eklendi (`app/layout.tsx` artık `<Providers>` ile sarmalı — `useSession` hook çağrılabilir) + 3 debug `console.log` temizliği
- `12b3c72` **gemini-3-flash-preview stable fallback** — preview model 404/quota olursa `gemini-2.5-flash`'a otomatik düşer

### 10. FrameOS referanslarının tamamen kaldırılması
- `17d0263` 5 skill rename (`frameos-*` prefix'leri kaldırıldı): ts-expert, css-expert, supabase-expert, nextjs-expert, pivotera-security
- `frameos-openai-expert` silindi (proje Gemini öncelikli, OpenAI minimal)
- `plans/01-frameos-merge.md` → `01-platform-merge.md` + "Tarihsel" etiketi
- 10 dosyada metin temizliği (UTF-8 NoBom encoding korundu)
- Memory dosyaları (`project_overview.md`, `user_profile.md`) PivotaraHub-only olarak güncellendi

---

## Yarım Kalanlar (Tier-2 Backlog)

### Race condition — 4 sayfa
PowerShell ile yarı-fix denendi, riskli olduğu için geri alındı. Manuel refactor lazım:
- `app/clients/[id]/intake/page.tsx:20`
- `app/clients/[id]/bio-analysis/page.tsx:17`
- `app/clients/[id]/competitor-analysis/page.tsx:39`
- `app/clients/[id]/video-performance/page.tsx:18`

Pattern: `loadData`'yı useEffect içine inline et + setState'leri `cancelled` flag ile sarmala.

### Kod kalitesi
- `useState<any>` yaygın → `types/database.ts` tiplerini kullan
- `loading.tsx` / `error.tsx` boundary'leri yok (her route segment için)
- `app/videos/[id]/page.tsx:200` `startProduction` dead code (iş kararı — "üretim pasif, sonra aktif")
- `VideoAnalysisForm` clientId değişiminde form reset yok
- Python subprocess (`python-instagram-scraper`, `competitor-scraper`): timeout dışında kill yok, double-resolve race ince riski

### Performans Tier-3
- `lib/llm/video-analysis.ts` — `generatePlanVariations` kaldır + ana prompt'ta `responseSchema` ile 3 plan enforce (~%40 Gemini token tasarrufu)
- `videos/page.tsx` — search index pre-compute (100+ kayıtta filter typing lag)

### Rate limit eksiği
`[G-02 KALAN]` — Rate limit eklenmemiş AI endpoint'leri: complete-analysis (eklendi), land-script, video-metadata, video-production/render, video-analysis/regenerate-plans

---

## Kararlar

- **Plan 01** (FrameOS × PivotaraHub Birleşimi) → tarihsel kabul edildi. Plan 02 ve 03 fiili yol haritasını taşıyor.
- **FrameOS bağı tamamen kesildi** — PivotaraHub bağımsız platform olarak ilerliyor.
- **Mock data yasağı** — `complete-analysis`'te Edirne emlak fake datası gerçek bug yaratıyordu (Plan 03 ile temizlendi). Bundan sonra scraper null dönerse zincir partial-fail eder, fake oluşturulmaz.
- **Agent özelleştirme prensibi:** Generic agent template'de duruyor, proje-özel bağlam ayrı bölüm olarak eklenir. Böylece bir proje agent'ı diğerine kopyalansa generic kısım korunur.
- **NEW: `<SafeHtml>` pattern** — AI HTML çıktısı render edilecekse mutlaka bu component (DOMPurify whitelist ile).

---

## Bilinen Borçlar (MEDIUM/LOW — log'a düştü)

- TypeScript strict mode: `npm run build` hata listesi henüz temizlenmedi
- Zod şemaları kısmi — tüm Gemini çıktılarına eklenmedi
- Sidebar'da hardcoded istatistikler (T-01)
- `next-intl` package.json'da var ama kullanılmıyor olabilir — kontrol edilmedi
- `@google/genai` + `@google/generative-ai` iki SDK eşzamanlı kullanılıyor — konsolidasyon ayrı plan

---

## Yarına Başlangıç

1. **FAZ 2 — Director AI + FFmpeg** başla:
   - `npm install fluent-ffmpeg yt-dlp-exec`
   - `lib/videoPreprocessor.ts` — FFmpeg frame extraction
   - `lib/directors/cinematicDirector.ts` — GPT-4o-mini Vision
   - `lib/directorAI.ts` — scene_director | script_rewrite | full_rewrite
   - `app/api/director/route.ts`, `app/api/tts/route.ts`, `app/api/download-video/route.ts`

   Alternatif: Önce **race condition manuel fix** (Tier-2 backlog, 4 sayfa) veya **`generatePlanVariations` Tier-3** (Gemini token tasarrufu %40) yap.

2. **Dev test:** `npm run dev` → tarayıcıda auth + rate limit + Plan 03 paralel akış doğrula. `/login` → callbackUrl, `/api/clients/[id]/complete-analysis` POST'ta `duration_ms` ve `partial_failures` field kontrol.

3. **`generatePlanVariations` Tier-3** — `lib/llm/video-analysis.ts` içindeki fallback'i kaldır, ana prompt'a `responseSchema` ekle. Gemini SDK `responseSchema` desteği var (`@google/generative-ai`), 4-5 dosya değişikliği.

---

## Aktif Plan Durumu

| Plan | Durum |
|------|-------|
| `plans/01-platform-merge.md` | Tarihsel — Plan 02/03 ile devam |
| `plans/02-form-konsolidasyon.md` | ✅ Tamamlandı (4 faz) |
| `plans/03-complete-analysis-parallel.md` | ✅ Tamamlandı (1 commit) |
